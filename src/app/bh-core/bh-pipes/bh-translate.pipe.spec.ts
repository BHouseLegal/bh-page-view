import { BhTranslatePipe } from './bh-translate.pipe';

describe('BhTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new BhTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
